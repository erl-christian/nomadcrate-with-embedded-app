<section class="nc-hero-section section-{{ section.id }}" aria-label="NomadCrate Advanced Hero">
  <div class="nc-hero-media-vault">
    {%- if section.settings.bg_image != blank -%}
      {{ section.settings.bg_image | image_url: width: 2200 | image_tag: 
        loading: 'eager', 
        sizes: '100vw', 
        widths: '375, 550, 750, 1100, 1500, 1780, 2200',
        class: 'nc-hero-img-asset'
      }}
    {%- else -%}
      <img 
        src="https://images.unsplash.com/photo-1501555088652-021faa106b9b?auto=format&fit=crop&w=2200&q=80" 
        class="nc-hero-img-asset" 
        loading="eager" 
        alt="NomadCrate Premium Adventure Background"
      >
    {%- endif -%}
    <div class="nc-hero-cinematic-overlay"></div>
  </div>

  <div class="nc-hero-viewport">
    <div class="nc-hero-master-grid">
      
      <div class="nc-hero-narrative">
        {%- if section.settings.tagline != blank -%}
          <div class="nc-hero-badge animate-item">
            <span class="nc-badge-dot"></span>
            <span class="nc-badge-text">{{ section.settings.tagline | escape }}</span>
          </div>
        {%- endif -%}

        <h1 class="nc-hero-editorial-title animate-item">
          <span class="nc-title-light">{{ section.settings.title_part_1 }}</span>
          <span class="nc-title-bold">{{ section.settings.title_part_2 }}</span>
        </h1>

        {%- if section.settings.subheading != blank -%}
          <p class="nc-hero-paragraph animate-item">
            {{ section.settings.subheading | escape }}
          </p>
        {%- endif -%}

        {%- if section.settings.btn_label != blank and section.settings.btn_link != blank -%}
          <div class="nc-hero-cta-group animate-item">
            <a href="{{ section.settings.btn_link }}" class="nc-btn-primary">
              <span class="nc-btn-label">{{ section.settings.btn_label | escape }}</span>
              <span class="nc-btn-arrow-wrapper">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
              </span>
            </a>
          </div>
        {%- endif -%}
      </div>

      <div class="nc-hero-interactive-wing">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'spotlight_card' -%}
              <div class="nc-glass-card animate-card" {{ block.shopify_attributes }}>
                {%- if block.settings.card_image != blank -%}
                  <div class="nc-card-thumb">
                    {{ block.settings.card_image | image_url: width: 300 | image_tag: loading: 'lazy', class: 'nc-thumb-asset' }}
                  </div>
                {%- endif -%}
                <div class="nc-card-details">
                  <span class="nc-card-tag">{{ block.settings.card_tag | escape }}</span>
                  <h3 class="nc-card-heading">{{ block.settings.card_title | escape }}</h3>
                  {%- if block.settings.card_link != blank -%}
                    <a href="{{ block.settings.card_link }}" class="nc-card-action-link">
                      {{ block.settings.card_link_label | escape }} 
                      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
                    </a>
                  {%- endif -%}
                </div>
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>

    </div>
  </div>
</section>

{% stylesheet %}
.nc-hero-section {
  position: relative;
  height: 100vh;
  height: 100svh; /* Modern small viewport height handles mobile browser bars beautifully */
  display: flex;
  align-items: center;
  overflow: hidden;
  background-color: #090d16;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

.nc-hero-media-vault {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.nc-hero-img-asset {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  animation: ncScaleKenBurns 24s ease-out forwards;
}

.nc-hero-cinematic-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    to right,
    rgba(9, 13, 22, 0.95) 0%,
    rgba(9, 13, 22, 0.8) 40%,
    rgba(9, 13, 22, 0.4) 100%
  );
}

.nc-hero-viewport {
  width: 100%;
  max-width: 1400px;
  margin: 0 auto;
  padding: 2rem; /* Tightened up layout space to prevent text spills on short displays */
  position: relative;
  z-index: 2;
  max-height: 100%;
  display: flex;
  align-items: center;
}

.nc-hero-master-grid {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 4rem;
  align-items: center;
  width: 100%;
}

.nc-hero-narrative {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.nc-hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.12);
  padding: 6px 14px;
  border-radius: 100px;
  margin-bottom: 1.5rem;
  backdrop-filter: blur(4px);
}

.nc-badge-dot {
  width: 6px;
  height: 6px;
  background-color: #f97316;
  border-radius: 50%;
  box-shadow: 0 0 10px #f97316;
}

.nc-badge-text {
  color: #e2e8f0;
  font-size: 0.85rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.nc-hero-editorial-title {
  color: #ffffff;
  line-height: 1.05;
  margin: 0 0 1.25rem 0;
  letter-spacing: -0.03em;
  display: flex;
  flex-direction: column;
}

.nc-title-light {
  font-size: clamp(2rem, 4.5vw, 4.5rem); /* Normalized clamps so typography doesn't break full-screen rules */
  font-weight: 300;
  color: #f1f5f9;
  font-family: "Playfair Display", "Didot", "Georgia", serif;
  font-style: italic;
}

.nc-title-bold {
  font-size: clamp(2.2rem, 5vw, 5rem);
  font-weight: 800;
  text-transform: uppercase;
  background: linear-gradient(180deg, #ffffff 0%, #cbd5e1 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.nc-hero-paragraph {
  color: #94a3b8;
  font-size: clamp(1rem, 1.5vw, 1.2rem);
  line-height: 1.5;
  max-width: 540px;
  margin: 0 0 2rem 0;
  font-weight: 400;
}

.nc-btn-primary {
  display: inline-flex;
  align-items: center;
  background-color: #ffffff;
  color: #090d16;
  text-decoration: none;
  padding: 4px 6px 4px 28px;
  border-radius: 100px;
  font-weight: 600;
  font-size: 0.95rem;
  letter-spacing: -0.01em;
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.4);
}

.nc-btn-arrow-wrapper {
  width: 44px;
  height: 44px;
  background-color: #f97316;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  margin-left: 18px;
  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.nc-btn-arrow-wrapper svg {
  width: 18px;
  height: 18px;
}

.nc-btn-primary:hover {
  background-color: #f8fafc;
  transform: translateY(-3px);
  box-shadow: 0 12px 40px rgba(249, 115, 22, 0.2);
}

.nc-btn-primary:hover .nc-btn-arrow-wrapper {
  transform: rotate(-45deg);
}

.nc-hero-interactive-wing {
  display: flex;
  justify-content: flex-end;
  align-items: center;
}

.nc-glass-card {
  background: rgba(15, 23, 42, 0.45);
  border: 1px solid rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  padding: 1.25rem;
  border-radius: 20px;
  display: flex;
  align-items: center;
  gap: 1.25rem;
  max-width: 360px;
  width: 100%;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
  transform: rotate(2deg);
  transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
}

.nc-glass-card:hover {
  transform: rotate(0deg) translateY(-8px);
  border-color: rgba(255, 255, 255, 0.18);
  box-shadow: 0 30px 60px rgba(249, 115, 22, 0.1);
}

.nc-card-thumb {
  width: 80px;
  height: 80px;
  background-color: rgba(255, 255, 255, 0.03);
  border-radius: 12px;
  overflow: hidden;
  flex-shrink: 0;
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.nc-thumb-asset {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.nc-card-details {
  display: flex;
  flex-direction: column;
}

.nc-card-tag {
  color: #f97316;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: 4px;
}

.nc-card-heading {
  color: #ffffff;
  font-size: 1.05rem;
  font-weight: 600;
  margin: 0 0 8px 0;
  line-height: 1.3;
}

.nc-card-action-link {
  color: #94a3b8;
  text-decoration: none;
  font-size: 0.85rem;
  font-weight: 500;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  transition: color 0.2s ease;
}

.nc-card-action-link:hover {
  color: #ffffff;
}

@keyframes ncScaleKenBurns {
  from { transform: scale(1.06); }
  to { transform: scale(1.0); }
}

.animate-item {
  opacity: 0;
  transform: translateY(25px);
  animation: ncSlideUpFade 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.nc-hero-narrative .animate-item:nth-child(1) { animation-delay: 0.1s; }
.nc-hero-narrative .animate-item:nth-child(2) { animation-delay: 0.25s; }
.nc-hero-narrative .animate-item:nth-child(3) { animation-delay: 0.4s; }
.nc-hero-narrative .animate-item:nth-child(4) { animation-delay: 0.55s; }

.animate-card {
  opacity: 0;
  transform: scale(0.9) rotate(5deg);
  animation: ncCardPopIn 1s cubic-bezier(0.16, 1, 0.3, 1) 0.6s forwards;
}

@keyframes ncSlideUpFade {
  to { opacity: 1; transform: translateY(0); }
}

@keyframes ncCardPopIn {
  to { opacity: 1; transform: scale(1) rotate(2deg); }
}

@media screen and (max-width: 1100px) {
  .nc-hero-master-grid {
    grid-template-columns: 1.1fr 0.9fr;
    gap: 2rem;
  }
}

@media screen and (max-width: 992px) {
  /* Enforces 100vh on Tablet & Mobile instead of dropping to auto height */
  .nc-hero-section {
    height: 100vh;
    height: 100svh;
    padding-top: 0;
  }
  
  .nc-hero-cinematic-overlay {
    background: linear-gradient(
      180deg,
      rgba(9, 13, 22, 0.9) 0%,
      rgba(9, 13, 22, 0.85) 60%,
      rgba(9, 13, 22, 0.95) 100%
    );
  }

  .nc-hero-master-grid {
    grid-template-columns: 1fr;
    gap: 2.5rem;
    text-align: center;
  }

  .nc-hero-narrative {
    align-items: center;
  }

  .nc-hero-editorial-title {
    align-items: center;
    margin-bottom: 1rem;
  }
  
  .nc-hero-paragraph {
    margin-bottom: 1.5rem;
  }

  .nc-hero-interactive-wing {
    justify-content: center;
  }
  
  .nc-glass-card {
    transform: rotate(0deg);
    max-width: 400px;
  }
  
  @keyframes ncCardPopIn {
    to { opacity: 1; transform: scale(1) rotate(0deg); }
  }
}

@media screen and (max-width: 480px) {
  .nc-hero-viewport {
    padding: 1.5rem;
  }
  
  .nc-btn-primary {
    width: 100%;
    justify-content: space-between;
  }

  .nc-glass-card {
    padding: 1rem;
    gap: 1rem;
  }

  .nc-card-thumb {
    width: 70px;
    height: 70px;
  }
}
{% endstylesheet %}

{% schema %}
{
  "name": "NomadCrate Advanced Hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Hero Background Image Layout"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Small Tagline Label",
      "default": "Premium Travel Equipment"
    },
    {
      "type": "text",
      "id": "title_part_1",
      "label": "Editorial Font Line (Italic)",
      "default": "Travel Smarter."
    },
    {
      "type": "text",
      "id": "title_part_2",
      "label": "Impact Bold Line",
      "default": "Pack Better."
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Context Subheading Description",
      "default": "Highly optimized dynamic luggage and storage configurations engineered for the modern technical explorer."
    },
    {
      "type": "text",
      "id": "btn_label",
      "label": "Call To Action Text",
      "default": "Explore Essentials"
    },
    {
      "type": "url",
      "id": "btn_link",
      "label": "Call To Action Target Destination",
      "default": "/collections/all"
    }
  ],
  "blocks": [
    {
      "type": "spotlight_card",
      "name": "Floating Feature Card",
      "limit": 1,
      "settings": [
        {
          "type": "image_picker",
          "id": "card_image",
          "label": "Featured Item Miniature Image"
        },
        {
          "type": "text",
          "id": "card_tag",
          "label": "Small Highlight Tag",
          "default": "Trending Highlight"
        },
        {
          "type": "text",
          "id": "card_title",
          "label": "Product System Title",
          "default": "Modular Tech Crate v2"
        },
        {
          "type": "text",
          "id": "card_link_label",
          "label": "Action Text Link",
          "default": "Configure Bundle"
        },
        {
          "type": "url",
          "id": "card_link",
          "label": "Product Core Destination URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "NomadCrate Advanced Hero",
      "blocks": [
        {
          "type": "spotlight_card"
        }
      ]
    }
  ]
}
{% endschema %}